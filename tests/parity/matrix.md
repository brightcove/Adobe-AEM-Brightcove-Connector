# Feature parity matrix — cloud vs on-prem

Phase 0 baseline (see `ONPREM-PARITY-PLAN.md` §3). One row per user-visible capability.
Cell values are exactly one of: `works` · `broken` · `absent` · `not measured`.
`not measured` is a legitimate value; never default it to `works`. Every `works`/`broken`
cell names the evidence file under `runs/<date>/<line>/` (screenshot, JSON, or curl output).
Public repo: no account, video or playlist IDs in this file (use `<video-id-X>` placeholders; the real ids are in the gitignored runs/ NOTES); evidence files live in `runs/`
which is gitignored.

| # | Capability | Where | cloud 7.2.3 @ :4502 | on-prem 6.0.12 @ :4602 | on-prem 7.x (later) | Notes |
|---|---|---|---|---|---|---|
| 1 | Connector bundle Active after install | OSGi console | works | works |  | on-prem: `com.coresecure.brightcove.cq5.brightcove-services 6.0.12 Active` (runs/2026-09-17/onprem-6.0.12/bundles-brightcove.txt); cloud: `brightcove.core 7.2.3 Active` (runs/2026-09-17/cloud-7.2.3/bundles-brightcove-after.txt); ⚠️ a stale 7.2.2 (orphaned jcrinstall entry) was co-Active until triage uninstalled it + refreshPackages; uninstall alone left `/bin/brightcove/accounts` 500 `bundleContext is null`; bundle symbolic name + version |
| 2 | Admin tool page loads (URL) | Brightcove Admin | works | works |  | on-prem: same URL `/brightcove/admin.html` (vanity path in extensions/console), 200 (runs/2026-09-17/onprem-6.0.12/admin-page.html); cloud URL `/brightcove/admin.html` (runs/2026-09-17/cloud-7.2.3/admin-videos.png); record the URL per line |
| 3 | Accounts servlet lists the configured account | `/bin/brightcove/accounts` | works | works (after fix) |  | on-prem: 1 account once `allowed_groups` includes a group admin is in (`everyone` on 6.5.0 GA; admin is NOT in `administrators` there); servlet is empty otherwise; cloud: 1 account (runs/2026-09-17/cloud-7.2.3/accounts.json) |
| 4 | Videos table populates | Admin | works | works |  | on-prem: smoke spec passes with real rows (runs/2026-09-17/onprem-6.0.12/e2e-report.json); cloud: rows rendered (runs/2026-09-17/cloud-7.2.3/admin-videos.png; api-search-videos.txt returns items); row count > 0 |
| 5 | Video search by text | Admin | works | works | | on-prem: search narrows results (totals 54→42 for a full video name as the query) confirming the query IS applied server-side; it is loose token matching ("In Every Field" splits on words), not exact-substring, which is expected Brightcove CMS search behavior, not a defect. Separately found: the header/panel title stays "ALL VIDEOS" after a search instead of "Search: &lt;term&gt;" because `searchVideoURL()` (brcAdmin.js) wires its JSONP callback to `showAllVideosCallBack` (hardcoded title) rather than the dedicated `searchVideoCallBack` — a real but cosmetic label-only bug. Evidence: matrix/row05-video-search-narrowed.png, row05-video-search.json cloud: search by text works — typed a real video's own name, request `a=search_videos&query=<term>` fired, table re-rendered filtered to the match (runs/2026-09-17/cloud-7.2.3/matrix/row05-search-before.png, row05-search-after.png, admin-readonly-results.json) |
| 6 | Sort videos (name/date; ID sort was removed on cloud BCON-190) | Admin | works | works (Name sort) |  | on-prem: bcon-183-190 Name-sort spec passes; ID header not sortable either (runs/2026-09-17/onprem-6.0.12/e2e-report.json) cloud: clicking Name (#nameCol) and Last Updated (#lastUpdated) both re-sort via a fresh server request, row count stable at 30 throughout (runs/2026-09-17/cloud-7.2.3/matrix/row06-sort-name.png, row06-sort-date.png, admin-readonly-results.json) |
| 7 | Paging through videos | Admin | works | works | | on-prem: 54 videos, page size 30, Page Number dropdown ("Page 1 (Videos 1 to 30)") renders and is populated once totals exceed the page size (runs/2026-09-17/onprem-6.0.12/matrix/row07-paging.png, row05-video-search-narrowed.png) cloud: 54 videos across 2 pages via the `select[name=selPageN]` Page Number control, page 2 shows a distinct 24-row set (runs/2026-09-17/cloud-7.2.3/matrix/row07-pagination-page1.png, row07-pagination-page2.png, admin-readonly-results2.json) |
| 8 | Filter panel (folder / label / state) | Admin | works | works | | on-prem: `#filter_clips` "Show Only Clips" checkbox correctly hid all 30 rows in a clip-free sample (0 visible) and restored them on uncheck; `#fldr_list`/`#label_list` selects populated with real data (5 real folders, 15 real labels + "+ Create New Label"); no separate "state" filter exists (row title's third facet), only the clips checkbox and the two selects (runs/2026-09-17/onprem-6.0.12/matrix/row08-filter-clips.png, row08-filter-selects.png, row08-filter-panel.json) cloud: filter panel opens (#filterPanel), 6 folders / 17 labels listed; filtering by an existing label (`/test/demo/`) narrowed 30 rows to 1 (runs/2026-09-17/cloud-7.2.3/matrix/row08-filter-panel-open.png, row08-filter-by-label.png, admin-readonly-results.json) |
| 9 | Video detail panel opens with metadata | Admin | works | works | | on-prem: clicking a row shows `#tdMeta` populated with real name/id/thumbnail/description/tags/labels/economics/published-date (runs/2026-09-17/onprem-6.0.12/matrix/row09-detail-panel.png, row09-detail-panel.json) cloud: clicking a row opens `#tdMeta`/`.brc-panel-inner` with a populated 10-row info table and header (name + updated date) (runs/2026-09-17/cloud-7.2.3/matrix/row09-detail-panel.png, admin-readonly-results.json) |
| 10 | Edit + save video metadata (name/description/tags) | Admin → CMS | not measured | absent | | re-read from CMS after save on-prem: no element in the served admin.html carries an onclick invoking `extMetaEdit()`/`metaEdit()` (grepped the full served DOM) — the detail panel renders name/description/tags as plain read-only `<div>` text with no Edit/Save affordance anywhere. The function itself still exists in `brcUI.js` and runs without throwing when invoked directly from the console, but produces no visible dialog (a `.x-window` DOM node exists but never renders, alongside 12 resource-404 console errors) — so even bypassing the missing UI trigger, the underlying code path does not actually work. Never clicked a "Send"/Save button: no CMS mutation attempted. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row10-edit-dialog-diag.png, row10-edit-dialog.json, row10-edit-dialog-diag.json, row10-console.json cloud: not measured — saving name/description/tags would mutate a live video and isn't in the allowed-mutation set (playlist rename / label apply-remove / folder move only). UI affordance confirmed present: 3 editable inputs render in the panel with real values (runs/2026-09-17/cloud-7.2.3/matrix/row10-metadata-panel.png, admin-viewonly-results.json) |
| 11 | Poster / thumbnail image URL update | Admin | not measured | works | | on-prem: "Change Poster" and "Change Thumbnail" links (wired via `onclick="uploadPoster()"`/`uploadThumbnail()` directly in the served markup) both open a real dialog with a URL text field; opened for screenshot evidence only, not submitted (posting a new image would mutate the live video and isn't in the allowed-mutation set). Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row11-poster-dialog.png, row11-thumbnail-dialog.png, row11-poster-thumbnail.json cloud: not measured for save — posting a new poster/thumbnail URL would mutate the live video and isn't an allowed mutation. Inline-edit affordance confirmed: ENTER URL opens the inline input (no popup, BCON-184 shape intact) and Cancel returns to button view with zero requests fired (runs/2026-09-17/cloud-7.2.3/matrix/row11-image-url-inline-edit.png, admin-viewonly-results.json) |
| 12 | Text tracks list + upload | Admin | works | works |  | on-prem: found a real video with a captions track (id <video-id-C>) via the API and confirmed the list renders it correctly in `#divMeta.text_tracks` (LABEL/LANGUAGE/TYPE/DELETE table, "Chinese (Simplified)"/zh-CN/captions); "Upload New Text Track" button (`#uploadtrackbutton`, wired via onclick in the served markup) opens a real dialog with language/label/type/default/source-URL fields plus a file picker. Opened for screenshot evidence only, not submitted (would add a permanent live text track, not an allowed mutation). Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row12-tracks-list-fixed.png, row12-upload-dialog.png, row12-text-tracks.json cloud: list confirmed live — walked rows, found one with a real track rendered in `#textTracksSection`, Upload button present (runs/2026-09-17/cloud-7.2.3/matrix/row12-text-tracks-list.png, admin-viewonly-results.json); upload itself not attempted (would add a permanent live text track; connector has `remove_text_track` but that's still an unlisted mutation for this pass) |
| 13 | Labels: list, create, apply to video | Admin | broken | broken |  | on-prem: the 2022 on-prem commits (a3010dc, b3b92cf, a09d242) DID add a real labels UI — `#label_list` filter select (15 real labels + "+ Create New Label"), and an "Edit Labels" dialog (`editLabels()`, wired via a real `onclick` next to "Labels:" in the detail panel) with autocomplete-driven add. List + apply both round-tripped correctly on video <video-id-B> (labels: [] → ["/test2/"], confirmed via a fresh `/bin/brightcove/api.js` re-read, not just the UI). But this hits the SAME defect the cloud team found independently on 7.2.3: removing a video's LAST label is a silent no-op. jQuery's `$.param([], true)` drops the `labels` param entirely when the array is empty, and the on-prem `update_labels` servlet treats an absent (or even an explicit empty-string) `labels` param as "no change" rather than "clear all" — confirmed by network capture (`labels` param missing from the outgoing request) and by retrying with an explicit `labels=` directly via curl (still a no-op). ⚠️ Residual live-account state: video <video-id-B> ("42min - sync") now has label `/test2/` that could not be removed via any client mechanism (UI or direct API) due to this bug — same class of cleanup issue the cloud team flagged for their own video. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row13-label-autocomplete.png, row13-label-added-in-dialog.png, row13-label-before-remove.png, row13-label-mutation.json cloud: apply + remove-with-others-remaining both round-trip correctly (verified via a fresh video-record re-fetch, not just the toast — `/bin/brightcove/api.json?isID=true&a=search_videos&query=<id>`). But removing the LAST remaining label is a silent no-op: jQuery drops the empty `labels` array from the outgoing querystring, `BrcApi#updateLabels` only acts when `labels` is present, so the server never runs the removal — yet the UI still toasts "Labels saved". Root cause + repro: runs/2026-09-17/cloud-7.2.3/matrix/row13-defect-summary.json; evidence: row13-label-pill-added.png, row13-label-pill-removed.png, row13-label-mutation-result.json. ⚠️ Residual live-account state: video <video-id-A> now has label `/test/demo/` that could not be removed via the connector due to this bug — needs manual cleanup via Brightcove Studio. |
| 14 | Language variants: view / create / edit | Admin | works | absent |  | on-prem: zero `coral-tab` in served admin markup; variants dialog is 7.x-only (bcon-176/177, runs/2026-09-17/onprem-6.0.12/NOTES.md §5) cloud: DAM asset editor's Brightcove tab loads clean (not flagged invalid), Add Variant dialog opens with the Name field correctly marked required (red asterisk); view-only, no variant created/edited (runs/2026-09-17/cloud-7.2.3/matrix/row14-brightcove-tab.png, row14-variant-dialog-open.png, row14-variant-view-result.json). Non-fatal: a console 404 on `/bin/brightcove/author/users/current-user-info` (unrelated auxiliary endpoint, didn't block the dialog) |
| 15 | Folders: list, move video to folder | Admin | works | works | | on-prem: moved video <video-id-C> to folder `aem_test_folder` and back to "[No Folder]" via the real `.folder-selector` menu (`#btn_MoveVideoToFolder` toggle + `data-folder-id` menu items, including a static "[No Folder]" option for unassigning) — both directions confirmed via a fresh `/bin/brightcove/api.js` re-read (folder_id: null → the target folder id → null). Fully reversible, unlike the cloud build where the equivalent "back to none" control doesn't exist. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row15-folder-menu-open.png, row15-after-move.png, row15-after-revert.png, row15-folder-mutation.json cloud: list-folders + move-to-folder both confirmed via a fresh video-record re-fetch (folder_id changed to the target folder). Moving a video BACK to All Videos has no reachable UI control in this build — the current `#moveToFolderModal`/`#mtfFolderList` only lists real folders (confirmed empty of a "No Folder" entry live), even though stale markup for a `.folder-selector` menu with `data-folder-id="none"`/"[No Folder]" still ships in admin.html and its handler still exists in brcUI.js — just unreachable from the current modal flow. Used the underlying `remove_video_from_folder` connector API action directly to restore the live account (confirmed back to folder_id null). Evidence: runs/2026-09-17/cloud-7.2.3/matrix/row15-folder-list.png, row15-move-modal-open.png, row15-after-move.png, row15-folder-mutation-result.json |
| 16 | Playlists: list all (pagination removed on-prem 2024-11) | Admin | works | works | | on-prem: 16 playlists rendered, matching `api.js?a=search_playlists` totals exactly (16), `#pagination` confirmed hidden (`buildPlaylistList()` explicitly sets `pagination` display:none) — matches the on-prem-2024-11 pagination-removal commits (3b27f2f/48b94cc). Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row16-playlists-all.png, row16-playlists-list.json cloud: Playlists tab lists 16 real playlists, no pagination control needed at this count (runs/2026-09-17/cloud-7.2.3/matrix/row16-playlists-list.png, admin-readonly-results.json) |
| 17 | Playlists: search by name | Admin | works | absent | | on-prem: `#selField_pl` has exactly two `<option>`s — "In Playlist ID" and "In Reference ID" — there is no name-search option in the DOM at all. Confirmed in code: `getFindPlaylistsURL()` (brcAdmin.js) only branches on `searchField === "find_playlist_by_id"` or `"find_playlist_by_reference_id"`; any other value falls through to the plain all-playlists URL. Search BY ID does work correctly (searched a real playlist's id, got exactly 1 matching row back). This refines the e2e baseline's "selector-or-behaviour" classification for `bcon-172-playlist-search.spec.js`: it isn't old-vs-new markup drift on a shared feature, name search has no code path on-prem at all. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row17-playlist-search-by-id.png, row17-playlist-search.json cloud: search by Name with a real term ("Smart") returned exactly the 2 matching playlists ("A Smart Playlist", "Smart Test"); a too-short/fragment term ("A", "A Sma") returned zero — Brightcove's playlist search appears to require a real token, not an arbitrary substring, which is expected search behavior, not a defect (runs/2026-09-17/cloud-7.2.3/matrix/row17-playlist-search-smart.png, admin-readonly-results2.json) |
| 18 | Playlists: create / rename / add-remove videos | Admin | works | absent (rename); create/add-remove UI present, not exercised | | on-prem: renaming a playlist has NO reachable UI path at all on 6.0.12. Code inspection of `editPlaylistListingCallback()` (brcUI.js, the handler behind the `.edit-playlist` pencil link) shows its dialog renders only a video-search-to-add form plus the current video listing (each removable); its Update handler posts only `{a: update_playlist, videos: [...], playlistId}` — no name/title field anywhere in that dialog or any other on-prem code path. "Create Playlist" (`#newplstButton`) also turns out to mean "create playlist FROM the currently-selected videos" (it lives in `.butDiv`, hidden until a video checkbox is checked), not a blank-form create — opened for screenshot evidence only. Neither dialog was submitted (create and add/remove-video are not on the pre-authorized mutation list; only rename+revert was pre-approved, and rename has nothing to exercise). Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row18-create-playlist-dialog.png, row18-edit-playlist-dialog.png, row18-playlist-crud.json cloud: renamed a real playlist ("A Smart Playlist" -> "A Smart Playlist (parity-check)") and back, each step confirmed via a fresh `a=search_playlists` re-read (not just the toast); playlist restored to its original name (runs/2026-09-17/cloud-7.2.3/matrix/row18-edit-modal-open.png, row18-after-rename.png, row18-playlist-mutation-result.json). Create-new-playlist and add/remove-videos-on-a-playlist were not exercised live (would leave persistent live objects/edits, outside the allowed-mutation set) |
| 19 | Bulk actions (select-all, bulk move/delete) | Admin | works | absent |  | on-prem: no `#bulkActionBar`/select-all (bcon-179-181, runs/2026-09-17/onprem-6.0.12/NOTES.md §5) cloud: selecting 2 rows shows `#bulkActionBar` with a correct count ("2 selected"); select-all (`#checkToggle`) checks all 30 visible rows (runs/2026-09-17/cloud-7.2.3/matrix/row19-bulk-bar.png, row19-select-all.png, admin-readonly-results.json). The actual bulk move/delete action itself was not fired (only a single-video folder move is in the allowed-mutation set) |
| 20 | Account switch (multi-account) + confirm dialog | Admin | not measured (single account) | switches without confirm |  | on-prem: account switch is immediate, no confirm dialog (bcon-178); only one account configured so switching itself not exercised cloud: only one account is configured (`/bin/brightcove/accounts` returns exactly 1). The confirm-dialog mechanism itself was exercised (popover opens, 1 account row) but a real cross-account switch could not be tested (runs/2026-09-17/cloud-7.2.3/matrix/row20-account-popover.png) |
| 21 | Upload video (ingest) + ingest-profile choice | Admin | absent | absent | | on-prem: same shape as the cloud finding — no button/link/onclick anywhere in the served admin.html invokes `extFormUpload()` (grepped the full rendered page, zero matches; the only reachable upload-related controls are the per-video poster/thumbnail image upload and a "Get Upload Status by ID" lookup that presupposes an out-of-band-created upload id). The Dynamic Ingest URL + Ingest Profile dropdown (with real profile options: Express Standard, Live - HD, balanced-nextgen-player, etc.) still exist as code in `extFormUpload()`, and calling it directly from the console runs without throwing, but produces no visible dialog either (dead in the same way row 10's edit dialog is) — so this is unreachable AND non-functional even bypassed, not just unreachable. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row21-ingest-dialog-isolated.png, row21-ingest.json cloud: no reachable UI entry point to upload/ingest a new video exists in admin.html (grepped the full rendered page: no "Upload Video"/"Ingest"/"New Video" control). The ingest-profile-picker markup and `a=create_video` wiring DO still exist in `brcUI.js` (`extFormUpload()`, an ExtJS form with the ingest-profile dropdown) but that function is never called from anywhere in the bundled JS or the rendered HTML — dead code, not a live path. The only "upload" affordances present are per-video poster/thumbnail image upload and text-track upload (different capabilities, rows 11/12). |
| 22 | Sync overlay / loading states | Admin | broken | works | | on-prem: exercised the `#loading`/`.loading`/`#syncdbutton` state machine via direct `syncStart()`/`syncEnd()` calls (avoided clicking `#syncdbutton` itself to skip an actual `/bin/brightcove/dataload` resync round trip on the shared account, since the CSS/DOM toggle path is identical either way) — button text correctly flips to "LOADING SYNC", `#loading` becomes visible during, hides again after; no page errors. Did not check whether `/bin/brightcove/dataload` itself returns a parseable body on-prem (the cloud-side empty-body bug is a separate question from the overlay mechanics this row is measuring). Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row22-sync-loading.png, row22-sync-loading.json cloud: overlay shows/hides correctly and the button re-enables (`#syncOverlay`, `#syncdbutton` state machine itself is fine) — but `/bin/brightcove/dataload` returns HTTP 200 with a **completely empty body** (confirmed via direct curl), and `syncDB()`'s success handler unconditionally does `data = $.parseJSON(data)` on that empty string, throwing an uncaught "Unexpected end of JSON input" on every sync (captured via `page.on('pageerror')`) (runs/2026-09-17/cloud-7.2.3/matrix/row22-sync-overlay.png, admin-readonly-results.json) |
| 23 | Video Player component: place, pick video, render | Sites editor | works | works |  | on-prem: opened the Touch UI editor, selected the placeholder Video Player component (overlay droptarget at `data-path=".../brightcove_player_top"`), clicked the toolbar "Configure" action (`button[data-action="CONFIGURE"]`) — a real dialog opens ("Brightcove Video Player", Media/Advanced tabs, Account pre-populated with the real account, Video/Player selects present). Editor interaction now measured, going beyond the baseline's server-side-render-only check. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row23-player-dialog.png, row23-player.json cloud: both Video Player instances mount and initialize video.js (`.brightcove-container video.vjs-tech` present for each); one plays successfully (edge.api 200), the other 403s (edge.api `VIDEO_CLOUD_ERR_VIDEO_NOT_PLAYABLE`) — that 403 traces to a specific stale/invalid video id (6074404269001) baked into the test page's `brightcove_player_mid` component config, not a connector defect (confirmed: same account/session, sibling player with a valid id plays fine). Test-page fixture worth refreshing. Evidence: runs/2026-09-17/cloud-7.2.3/matrix/row23-27-editor-loaded.png, row23-27-sites-editor-result.json |
| 24 | Playlist Player component: place, pick playlist, render | Sites editor | works | works |  | on-prem: same interaction as row 23 on the Playlist Player component — "Brightcove Playlist Player" dialog opens with Account/Playlist/Player fields. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row24-playlist-player-dialog.png, row24-playlist-player.json cloud: Playlist Player component (`.brightcoveplayer-playlist`) renders real markup (>500 chars, includes the player CSS/container) (runs/2026-09-17/cloud-7.2.3/matrix/row23-27-sites-editor-result.json) |
| 25 | Experiences (in-page experience) component | Sites editor | works | works |  | on-prem: same interaction as row 23 on the Experiences component — "Brightcove Experience Selector" dialog opens with Account & Experience Configuration, Container ID/Class, and Experience Styles sections. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row25-experiences-dialog.png, row25-experiences.json cloud: Experiences component (`.brightcoveexperiences`) renders real markup (runs/2026-09-17/cloud-7.2.3/matrix/row23-27-sites-editor-result.json) |
| 26 | Component edit refreshes without full page reload (BGS-1690) | Sites editor | works | works (symptom not visible; mechanism present) |  | on-prem: `_cq_editConfig.xml` still uses `REFRESH_PAGE` (3 files; cloud has 0 REFRESH_PAGE / 4 REFRESH_SELF), and the ContentFrame iframe DID reload on save (inner token lost), but the 6.5.0 Touch UI shell kept `#ContentScrollView.scrollTop` (350->350) so the user-visible BGS-1690 jump does not reproduce here. The listener port is still required for parity; the shell difference is why the symptom hid. on-prem: ⚠️ surprising result, contrary to the plan's expectation that this build would show the pre-BGS-1690 full-reload symptom. Forced the outer `#ContentScrollView` container to `scrollTop=350` (well below the fold, past the off-screen `brightcove_player_mid` component per the fixture's own design), opened its Configure dialog, saved via `.cq-dialog-submit` with no field changes. Result: `scrollTop` unchanged (350 → 350, confirmed both numerically and by an identical before/after screenshot) and the outer `editor.html` shell itself never navigated (a `window.__marker` set on the top document survived). The `ContentFrame` iframe's own JS context DID reset (a token set in its `window` was gone after save, meaning the iframe's document was refreshed), but that inner refresh does not disturb the outer viewport scroll position the user actually sees — so the user-visible BGS-1690 symptom (scroll jumping to top) does not reproduce on this on-prem build. This may mean the on-prem AEM Touch UI shell's own scroll-preservation (independent of the connector) already covers this, or that the connector-side full-reload trigger BGS-1690 fixed on cloud isn't present in 6.0.12's simpler dialog flow. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row26-scrolled-350.png, row26-after-save-v4.png, row26-bgs1690.json cloud: isolated, controlled test on the healthy top player — scrolled 1600px, set `window.__marker`, opened Configure on `brightcove_player_top`, saved: scroll delta 0px (well within the 50px budget), page did not reload (marker survived), zero page errors. Repeating the identical edit+save on `brightcove_player_mid` (the player with the pre-existing bad-video error, row 23) throws two uncaught exceptions during the refresh ("Cannot read properties of undefined (reading 'options')", "Invalid target for null#trigger; must be a DOM node or evented object.") — a real, separate, reproducible edge-case bug: the refresh path doesn't guard against re-triggering a video.js instance that failed its initial setup. Worth its own ticket; does not indicate BGS-1690 itself is broken. Evidence: runs/2026-09-17/cloud-7.2.3/matrix/row26-top-player-after-save.png (clean control), row26-configure-dialog-open.png, row26-after-save.png (error dialog on the bad-video player), row23-27-sites-editor-result.json |
| 27 | Content Finder / Asset Finder Brightcove tab | Sites editor | works | absent | | on-prem: the on-prem contentfinder extension (`apps/brightcove/extensions/contentfinder/*.js`, `.content.xml` declares `extensionType="contentfinder_extension"`, xtype `contentfindertab`) targets the classic CQ5-era ExtJS Content Finder sidekick widget model, which the modern AEM 6.5 Touch UI Sites editor does not load at all. Opened the modern Assets rail (`button.toggle-sidepanel`) and enumerated every visible tab: none is "Brightcove" (real tabs seen: Publish status, Asset type, Last Modified, Filetype, Size, Product status, Form Model, Create, Target, Goals & Settings). Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row27-sidepanel-open.png, row27-content-finder.json, row27-tabtexts-icon1.json cloud: the Sites editor's Assets rail Category dropdown includes a "Brightcove Videos" source; selecting it populated 20 real account video thumbnails with filenames/dimensions (runs/2026-09-17/cloud-7.2.3/matrix/row27-brightcove-videos-category.png, row27-content-finder-result.json) |
| 28 | DAM asset: Brightcove metadata tab on video asset | Assets | works | not measured (no synced asset) | | on-prem: `/content/dam/brightcove_assets` returns 404 — no DAM asset has ever been synced on this fresh instance, so there is no Brightcove video asset to open a metadata tab on. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row28-dam-assets-404.txt cloud: DAM asset (`<video-id-B>.mp4`) Properties page Brightcove tab loads with 18 metadata fields rendered (runs/2026-09-17/cloud-7.2.3/matrix/row28-dam-brightcove-tab.png, row28-dam-asset-result.json) |
| 29 | DAM asset: sync to Brightcove via workflow model | Assets / workflow | not measured (model present, not executed) | not measured (model present, not executed) | | model names differ per line on-prem: `brightcove-sync-asset-workflow` model confirmed present under both `/conf/global/settings/workflow/models` and `/var/workflow/models`; backing OSGi component `com.coresecure.brightcove.wrapper.workflow.BrightcoveSyncAssetWorkflowStep` is Active. Not executed: running it would sync into the live shared account, outside the allowed-mutation set. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row29-30-workflow-models-conf.json, row29-30-workflow-models-var.json, row31-35-components.json cloud: `bc-sync-new-asset` workflow model confirmed present under both `/conf/global/settings/workflow/models` and `/var/workflow/models` (runs/2026-09-17/cloud-7.2.3/matrix/workflow-models-global.json, workflow-models-var.json). Not executed: running it would sync a new/existing asset into the live account, which is outside the allowed-mutation set for this pass. |
| 30 | DAM asset: delete-asset workflow | Assets / workflow | not measured (model present, not executed) | not measured (model present, not executed) | | on-prem: `brightcove-delete-asset-workflow` model confirmed present under both `/conf/global/settings/workflow/models` and `/var/workflow/models`; backing component `com.coresecure.brightcove.wrapper.workflow.BrightcoveDeleteAssetWorkflowStep` is Active. Not executed: it deletes, which the hard rules forbid. Evidence: same as row 29 cloud: `brightcove-delete-asset` workflow model confirmed present under both `/conf/global/...` and `/var/workflow/models` (same evidence as row 29). Not executed: it deletes, which the hard rules explicitly forbid. |
| 31 | Publish listener: replication triggers Brightcove sync | Replication | not measured (not fired) | not measured (not fired) | | on-prem: `com.coresecure.brightcove.wrapper.webservices.BrcReplicationHandler` is Active per `/system/console/components.json` (state field only). No publish tier running locally, so an actual replication event could not be fired to observe the sync side-effect. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row31-35-components.json cloud: `com.coresecure.brightcove.wrapper.listeners.BrightcovePublishListener` is Active per `/system/console/components.json` (runs/2026-09-17/cloud-7.2.3/matrix/components-full.json — see grep for 'brightcove'). No publish tier is running locally (port 4503 not up) so an actual replication event could not be fired to prove the sync side-effect. |
| 32 | Folder sync on publish path (BGS-1600) | Replication | not measured (not fired) | not measured (not fired) | | on-prem: same constraint as row 31 — `BrcReplicationHandler` Active, but folder-sync-on-publish needs a real publish event to observe; no publish tier running locally. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row31-35-components.json cloud: same constraint as row 31 — `BrcReplicationHandler` is Active (components-full.json) but folder-sync-on-publish (BGS-1600) needs a real publish event to observe. |
| 33 | Duplicate-video guard on redelivered publish (BGS-1705) | Replication | not measured | not measured (not fired) | | on-prem: no publish tier running locally to construct a redelivered-publish scenario; would need a running publish instance as a fixture. cloud: no publish tier running locally to construct a redelivered-publish scenario for the BGS-1705 duplicate-video guard; would need a running publish instance as a fixture. |
| 34 | Scheduled asset integrator (cron import of CMS videos into DAM) | Scheduler | not measured (not triggered) | not measured (not fired) | | on-prem: `com.coresecure.brightcove.wrapper.schedulers.asset_integrator.AssetIntegratorCronBundle` is Active per `/system/console/components.json` (state field only) but no scheduled run was fired/observed. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row31-35-components.json cloud: `AssetIntegratorCronBundleImpl` component is Active (runs/2026-09-17/cloud-7.2.3/matrix/row34-asset-integrator-component.json) but has no factory OSGi config instance on this AEM (`configMgr` search for its factory PID returned zero configs) — i.e. no schedule is actually wired up to fire it, so a cron-triggered import could not be observed. |
| 35 | `AEM_NO_DAM` tag filter respected on import | Scheduler | not measured (not triggered) | not measured (not fired) | | on-prem: same bundle as row 34 (Active); the `AEM_NO_DAM` tag filter can't be observed without a triggered scheduled run. cloud: same bundle as row 34 — the `AEM_NO_DAM` tag filter can't be observed without a triggered scheduled run. |
| 36 | Metadata PATCH to CMS on Java 21 (BGS-1600 fix) | Runtime | not measured | not measured | | on-prem needs a Java 21 boot on-prem: confirmed running on Java 11.0.23 (`/system/console/status-System Properties.txt`, `java.version` line) and 6.0.12 predates the BGS-1600 fix — the expected result under a Java 21 boot is `broken`, per the plan; not booted under Java 21 for this pass. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row36-system-properties.txt cloud: needs a Java 21 boot fixture for this AEM instance to exercise the BGS-1600 metadata-PATCH fix; this SDK author wasn't rebooted under Java 21 for this pass. |
| 37 | Proxy server honoured (incl. rendition creation) | Runtime | not measured | not measured | | needs a proxy fixture cloud: needs a proxy-server fixture in front of this AEM to exercise proxy-honoured rendition creation; none configured for this pass. |
| 38 | Context-path prefixed URLs in admin + contentfinder JS | Runtime | not measured | not measured | | needs a context root fixture cloud: needs a context-root/dispatcher fixture; this AEMaaCS SDK author runs at the root context, so context-path prefixing in the admin/contentfinder JS can't be exercised — matches the plan's own note that this only matters behind an on-prem dispatcher. |
| 39 | OSGi config: account factory config keys accepted | Config | works | works, different PID |  | on-prem factory PID is `...sling.BrcServiceImpl` (component name in `@Component`), keys snake_case; a config posted under the cloud PID `...ConfigurationServiceImpl` is silently ignored; keys differ per line, see plan §1.3 cloud: OSGi config accepted under factory PID `com.coresecure.brightcove.wrapper.sling.ConfigurationServiceImpl` (instance PID suffix redacted-safe: `...ConfigurationServiceImpl.e68551b0-...`). camelCase keys accepted: accountAlias, accountId, clientId, clientSecret, allowedGroups, playerStorePath, defaultVideoPlayerId, defaultVideoPlayerKey, defaultPlaylistPlayerId, defaultPlaylistPlayerKey, proxyServer, damIntegrationPath, defaultIngestProfile, defaultTagInclude (key names only; values redacted in the evidence file — the raw configMgr response briefly contained a plaintext `clientSecret` before redaction). Evidence: runs/2026-09-17/cloud-7.2.3/matrix/configmgr-search.json (redacted) |
| 40 | Service user / repoinit provisioning | Config | works | works | | on-prem: a real `rep:SystemUser` exists at `/home/users/system/G0QXXR_brightcove` (found directly via the `nodename=*brightcove*` querybuilder filter, unlike the cloud build's hashed node name), matching the on-prem package's repoinit filter. `/content/dam/brightcove_assets` does NOT exist yet (404) — expected on a fresh instance with no synced asset (row 28), not a provisioning defect; the folder is created on first sync, not at install time. Evidence: runs/2026-09-17/onprem-6.0.12/matrix/row40-system-user.json, row28-dam-assets-404.txt cloud: `brightcove_admin` service user confirmed as a real `rep:SystemUser` at `/home/users/system/j4zbiF0rWIkF72_4GH09` (`rep:authorizableId=brightcove_admin`) — not discoverable via the querybuilder `nodename` filter (hashed node name), found via `rep:authorizableId` property search instead; note this for future probes. `/content/dam/brightcove_assets` exists with real synced content (jcr:title "Brightcove Connector", processingProfile, metadataSchema wired). Evidence: runs/2026-09-17/cloud-7.2.3/matrix/service-user-brightcove_admin.json, dam-brightcove-assets-2.json |
| 41 | Ajile-free `Load()` transport (BGS-1706) | Admin JS | works | works (via ajile) |  | on-prem: `Load()` defined and all 3 bgs-1706 specs pass; 6.0.12 still ships the ajile vendor file (runs/2026-09-17/onprem-6.0.12/e2e-report.json); cloud: `typeof window.Load === 'function'`; js.txt lists brcTransport.js, no ajile (runs/2026-09-17/cloud-7.2.3/clientlib-tools-js.txt); `typeof window.Load === 'function'`, no pageerror |
| 42 | e2e suite: pass / fail / skipped counts | tests/e2e | 35 pass / 0 fail / 0 skip (after spec repair) | 6 pass / 28 fail / 1 skip (7.x-targeted suite on 6.0.12 UI) |  | on-prem run 4 = baseline runs/2026-09-17/onprem-6.0.12/e2e-report.json (runs 1-3 invalid: shared state, wrong PID + groups, blanked creds; see NOTES.md there); cloud gate: runs/2026-09-17/cloud-7.2.3/e2e-report.after-spec-repair.json; 6 rotted specs updated (spec-repair-NOTES.md), each proven red on a flipped assertion; first clean run was 28/34 with 6 spec-rot (triage/NOTES.md); |
